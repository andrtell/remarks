__Example library__

`./easymath.h`

```c
#ifndef EASYMATH_H
#define EASYMATH_H

int easymath_add(int a, int b);

#endif
```

`./easymath.c`

```c
int easymath_add(int a, int b) {
	return a + b;
}
```

_Given these two files we can compile a shared object file._

```
> cc -fPIC -shared -o libeasymath.so easymath.c
```

_Lets inspect the output_

```
> ls

easymath.c
easymath.h
libeasymath.so*

> file libeasymath.so

libeasymath.so: ELF 64-bit LSB shared object, x86-64, version 1 (SYSV), dynamically linked,
BuildID[sha1]=39fec9a5cb0e0b6d41f4407918906e17ba18b192, not stripped
```
